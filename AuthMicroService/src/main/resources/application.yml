server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AuthDB
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
