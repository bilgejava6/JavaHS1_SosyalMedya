server:
  port: 9091

spring:
  amqp:
    deserialization:
      trust:
        all: true
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: root
  data:
    mongodb:
      host: localhost
#      host: service-clusterip-mongo
      port: 27017
      username: admin
      password: root
      database: UserProfileDB
